import type { GenerateRequest, GenerateResponse } from '@/lib/types';
export async function generateAdventure(input: GenerateRequest): Promise<GenerateResponse> { const controller = new AbortController(); const timeout = window.setTimeout(() => controller.abort(), 12000); try { const response = await fetch('/api/adventure', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(input), signal: controller.signal }); const data = await response.json() as GenerateResponse; if (!response.ok) throw new Error(data.error || 'Could not generate adventure'); return data; } finally { window.clearTimeout(timeout); } }
